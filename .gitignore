# node
node_modules/
dist/
dist-electron/
release/
*.log

# python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# local database + user data (never commit a villager's farm data)
*.db
*.db-journal
*.db-wal
*.db-shm
apps/api/data/
data/lgd/dump/
# the development sync server's store holds other people's shared records
apps/sync/data/

# env / secrets
.env
.env.*
!.env.example

# os
Thumbs.db
.DS_Store

# Fonts fetched by scripts/fetch_fonts.py, and the faces extracted from the
# Windows-bundled Nirmala UI collection. Both are reproducible on any machine
# and neither belongs in the repository.
/data/fonts/

# Generated project reports are a farmer's own records, not source.
/apps/api/data/reports/

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
pydantic>=2.10.0
pydantic-settings>=2.6.0
# Project report (DPR) rendering. fpdf2 embeds the font and draws the tables;
# uharfbuzz shapes Indic scripts so matras and conjuncts land correctly;
# fonttools reads a font's character map and unpacks the Windows-bundled
# Nirmala UI collection into a face fpdf2 can embed.
fpdf2>=2.8.1
# Profile photos and equipment pictures: checked, stripped of EXIF (which can
# carry the GPS position of a farmer's house) and resized before storing.
# fpdf2 already depends on it; listed here because the app now uses it directly.
pillow>=10.4
uharfbuzz>=0.39
fonttools>=4.55
